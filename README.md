# LLM Synthetic Dataset Generator

This project automates the generation of synthetic regression datasets using an LLM.

Goal:
- Partition the (R²_knn, R²_lr) performance space into a 5x5 grid
- Generate 10 accepted datasets per cell
- Store accepted and rejected generations
- Track hit rates and generation efficiency

